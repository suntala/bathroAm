extends restaurant

block primary
    p
    each set, index in restosByNeighborhood
        p 
            strong #{neighborhoods[index]}
        ul
            each resto in set
                li 
                    a(href='/restaurant/'+resto.id) #{resto.name}
    //- pre= JSON.stringify(restosByNeighborhood, null, 2)



//- block primary
//-     p
//-     each set, index in restosByNeighborhood
//-         p 
//-             strong #{neighborhoods[index]}
//-         ul
//-             each resto in set
//-                 li 
//-                     a(href='/restaurant/'+resto.id) #{resto.name}






    //- p 
    //-     strong CHARLOTTENBURG
    //- each resto in charlottenburg
    //-     ul
    //-         a(href='/restaurant/'+resto.id) #{resto.name}

    //- p 
    //-     strong FRIEDRICHSHAIN
    //- ul
    //-     each resto in friedrichshain
    //-         li 
    //-             a(href='/restaurant/'+resto.id) #{resto.name}
        



//- extends restaurant

//- block primary
//-     p 
//-         strong CHARLOTTENBURG
//-     each resto in charlottenburg
//-         ul
//-             a(href='/restaurant/'+resto.id) #{resto.name}

//-     p 
//-         strong FRIEDRICHSHAIN
//-     ul
//-         each resto in friedrichshain
//-             li 
//-                 a(href='/restaurant/'+resto.id) #{resto.name}
        
//-     p 
//-         strong KREUZBERG
//-     each resto in kreuzberg
//-         ul
//-             a(href='/restaurant/'+resto.id) #{resto.name}

//-     p 
//-         strong MITTE
//-     each resto in mitte
//-         ul
//-             a(href='/restaurant/'+resto.id) #{resto.name}
    
//-     p 
//-         strong MOABIT
//-     each resto in moabit
//-         ul
//-             a(href='/restaurant/'+resto.id) #{resto.name}

//-     p 
//-         strong NEUKOLLN
//-     each resto in neukolln
//-         ul
//-             a(href='/restaurant/'+resto.id) #{resto.name}
    
//-     p 
//-         strong PRENZLAUER BERG
//-     each resto in prenzlauerBerg
//-         ul
//-             a(href='/restaurant/'+resto.id) #{resto.name}
    
//-     p <strong>SCHONEBERG</strong>
//-     each resto in schoneberg
//-         ul
//-             a(href='/restaurant/'+resto.id) #{resto.name}
